package TestCasesTest;

import PagesTest.LoginPage;
import UtilTest.Fakerdata;
import org.openqa.selenium.support.PageFactory;
import org.testng.annotations.Test;


//@Listeners(UtilTest.ListenerTest.class)
/*public class VerifyInvalidLogin extends BaseClass{
       @Test
    public void InLogin() throws InterruptedException {
        LoginPage invlogin = PageFactory.initElements(driver, LoginPage.class);
        invlogin.LoginFB(Fakerdata.TestName(), Fakerdata.TestPass());
        //Assert.assertTrue(false);
        Thread.sleep(5000);
           //WebDriverWait wait=new WebDriverWait(driver, 30);
           //wait.until(ExpectedConditions.);
    }
}*/